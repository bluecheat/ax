# 0.1.6 — skill 명명 일관화 + null-guard + doctor disclosure + plugin perspective cleanup

> 동작 변경 없는 cleanup + 일관성 보강.

## 1) Cross-skill drift 정정 4 건

- **starter preset hooks `set -e` 일관**: default preset 만 0.1.5 에서 fix 되고 starter preset (`spirit-check.sh`, `lint-changed.sh`) 누락 — `set -uo pipefail` 로 통일
- **`spec-check` → `spec-validate` global rename**: 8 파일에 15 occurrences 잔존 (skill 자체는 이미 spec-validate 로 rename 됐는데 다른 skill 의 prose 참조가 stale)
- **`adr-write` → `adr` global rename**: 2 파일 (triage/SKILL.md, risk-matrix.md)
- **audit/SKILL.md 가드 위반 예시**: plugin shipped `security.md` 직접 갱신 → project-specific `<project>-security.md` 신설 + @import (가드 #6 일관)

## 2) `skill_calls` null-guard 일괄 (6 skills)

`spirit, spec, spec-validate, spec-plan, spec-tasks, spec-implement` 의 `.skill_calls += 1` → `.skill_calls = ((.skill_calls // 0) + 1)`. fresh `state.json` (`null`) 에서 silently null 유지되던 버그 차단. 4 skills (`doctor, audit, install, rules`) 는 이미 null-guard 형태였음.

## 3) doctor drift 범위 disclosure

`doctor/SKILL.md` § 3.5 에 한 줄 추가 — `check-templates-drift.sh` 가 `_templates/spec/` 만 커버하고 `spirit/rules/`·`_templates/{adr,module,spirit}/` 는 미커버임을 사용자에게 명시. onboarding 절대 금지 항목과 cross-reference.

## 4) Plugin perspective cleanup

plugin 사용자는 항상 최신 버전 사용 — 본문에 박힌 dev-history 마커는 노이즈. 일괄 정리:

- 버전·history 마커 (`(0.1.X 발견)`, `(NEW)` 등) 제거
- `doctor/SKILL.md` 예시 출력의 `(goax 0.1.X)` → `<version>` placeholder
- `templates/default/.ax/spirit/values.md` historical comment 제거
- 가드 인용에서 버전 마커 제거 → 그냥 "onboarding 절대 금지 항목" 인용

## 검증

- grep sweep (skills/templates): 버전·history 마커 모두 0 건
- bulk rename 잔재 0 건 (`spec-check`, `adr-write`, `set -euo`, `skill_calls += 1`)
- 17 파일 수정, +38/-39 net
- skills 총 14 개

자세히: [`changelog/0.1.5.md`](0.1.5.md)
