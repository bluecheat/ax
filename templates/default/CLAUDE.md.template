# Claude Code Constitution — AGENTS.md alias

> 이 파일은 **SSOT 가 아니에요**. 본문은 [`AGENTS.md`](AGENTS.md) 에 있고, 이 파일은 Claude Code 의 import 트리거 역할만 해요.
> 직접 편집하지 말고 `AGENTS.md` 를 customize 하세요. doctor 가 두 파일 drift 를 감지해요.

@AGENTS.md

---

> **Multi-CLI 호환 노트**
> - Claude Code: 이 파일 자동 인식 → `@AGENTS.md` import 체인으로 본문 로드
> - OpenCode: `AGENTS.md` 를 직접 인식 (이 파일은 fallback). `opencode.json` 의 `instructions:` 도 `AGENTS.md` 우선
> - 호환 매트릭스·환경별 차이: [`.ax/docs/reference/opencode-compat.md`](.ax/docs/reference/opencode-compat.md)
