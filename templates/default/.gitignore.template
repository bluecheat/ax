# goax — runtime/임시 산출물 (per-machine, per-session)
.ax/state.json
.ax/current-task.json

# 재생성 캐시 (build-memory.sh — markdown·상태에서 결정론 재생성)
.ax/MEMORY.md

# install/onboarding 시점 머지 임시본 (사용자가 처리 후 rm)
.ax/*.suggested
.ax/**/*.suggested
.ax/.onboarding-pending

# hook 런타임 마커 (세션당 재생성)
.ax/.triage-nudged
.ax/.session/
.ax/.ablation-last

# 파일 쓰기 락 (goax_lock — mkdir 원자성. 디렉토리만, 파일형 *.lock 은 Cargo.lock 을 삼켜요)
*.lock/
