schema_version: "1.1"
name: critical-canary
tags: [rules, canary, hooks]
# goax + hook 미러 shim (tests/eval-hook-shim/README.md) — 샌드박스는 프로젝트 settings.json 을 안 읽어요
plugins: ["../..", "../../tests/eval-hook-shim"]
context:
  scaffold_script: scaffold.sh   # provision.sh 로 .ax/ 설치 + SP-SEC-001 룰 + src/signup.ts 픽스처
